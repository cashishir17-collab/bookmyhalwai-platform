import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact BookMyHalwai | Vendor Onboarding Support",
  description: "Contact BookMyHalwai for vendor onboarding, GST, FSSAI, and marketplace launch support.",
};

export default function ContactLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
